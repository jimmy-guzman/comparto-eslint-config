module.exports = {
  rules: {
    // https://eslint.org/docs/rules/prefer-arrow-callback (fixable)
    'prefer-arrow-callback': 'error',

    // https://eslint.org/docs/rules/prefer-const
    'prefer-const': 'error'
  }
}
